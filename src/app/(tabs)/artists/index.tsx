import { defaultStyle } from '@/styles'
import { Text, View } from 'react-native'

const ArtistsScreen = () => {
	return (
		<View style={defaultStyle.container}>
			<Text style={defaultStyle.text}>Artists Screen</Text>
		</View>
	)
}

export default ArtistsScreen
